# Own reusable privacy services and UI in helto-privacy

`helto-privacy` owns any privacy-domain capability that benefits from shared
implementation, including backend services and browser UI rather than only
cryptographic primitives. Consumer packs retain their product behavior and the
minimum integration needed to describe pack-specific data, preventing shared
privacy semantics from drifting across repositories.
